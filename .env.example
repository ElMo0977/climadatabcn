# ==============================================
# Meteo BCN - Environment Variables Example
# ==============================================
# Copy this file to .env and fill in the values

# Supabase (auto-configured by Lovable Cloud)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# API Proxy URL (optional, for CORS handling / hiding API keys)
# If set, requests will go through this proxy instead of direct to providers
# Pending: Create serverless proxy function to handle this
VITE_API_PROXY_URL=

# Debug: set to 1 to log request params and dataset stats (n points, step, duplicates, min/max) in console
# See docs/xema-transparencia-implementation.md
# VITE_DEBUG_DATA=0

# Debug XEMA diagnostics (opt-in, disabled by default)
# Set to true (or 1) to enable diagnostics
VITE_DEBUG_XEMA=false
